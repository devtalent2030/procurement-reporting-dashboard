# 1. Run the Data Generator (Creates the raw mock data)
python3 scripts/data_generator.py

# 2. Run the Anomaly Detector (Reads raw data -> Adds AI logic -> Saves processed data)
python3 scripts/anomaly_detector.py